name: Scrape emploi du temps (6 mois)

on:
  schedule:
    # 1er janvier à 02:00 UTC
    - cron: '0 2 1 1 *'
    # 1er août à 02:00 UTC
    - cron: '0 2 1 8 *'

  # Optionnel : déclenchement manuel si besoin
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 Install dependencies
        run: |
          pip install -r requirements.txt

      - name: 🚀 Run scraper
        env:
          CELCAT_LOGIN_URL: ${{ secrets.CELCAT_LOGIN_URL }}
          CELCAT_USERNAME: ${{ secrets.CELCAT_USERNAME }}
          CELCAT_PASSWORD: ${{ secrets.CELCAT_PASSWORD }}
        run: |
          python scraper/scrape_celcat.py

      - name: 💾 Commit generated files
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"
          git add output/
          git commit -m "📅 Mise à jour emploi du temps (auto)" || echo "No changes"
          git push
